begin(factorial_recursivo)

int fx factorial(int n) {
    var int res, temp, n2;
    if(n == 0){
        res = 1;
    }
    else{
        n2 = n - 1;
        temp = factorial(n2);
        res = n * temp;
    }
    return res;
}

main(){
    var int n, m;
    var string a;
    a = "Ingresa el número de para calcular su factorial";
    output(a);
    input(n);
    output("El factorial de ");
    output(n);
    output("es: ");
    m = factorial(n);
    output(m);
}

end(factorial_recursivo)
